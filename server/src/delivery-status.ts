export enum Status {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  IN_PROGRESS = 'in_progress',
  COLLECTED = 'collected',
  FINISHED = 'finished',
  REJECTED = 'rejected',
  EXPIRED = 'expired'
}
